# Claude Code continuation notes

Read `HANDOFF_CLAUDE_CODE.md` first.

Project root:

```powershell
C:\Users\dvill\Projeto_Fabrica_Tramontina_22399036
```

The user is building a factory-project package for the Tramontina BBQ kit SKU `22399036`.

Core rule: all calculations must be transparent and generated by code.

Do not build PPTX/slides yet. The user wants all technical material, calculations, diagrams, sources and HTML fronts finished before slides.

Regenerate everything with:

```powershell
.\scripts\run_all.ps1
```

Main files to inspect:

- `data\projeto.json`
- `scripts\generate_outputs.py`
- `scripts\build_workbook.mjs`
- `06_dashboard\index.html`
- `README.md`
- `HANDOFF_CLAUDE_CODE.md`

Important current UX issue already addressed:

- The central dashboard cards should open rendered HTML pages under `06_dashboard\entregaveis\`, not raw `.drawio` XML or raw Markdown.
- Raw editables are still available as download buttons inside those rendered pages.

